# CppCMakeProject

A simple C++ project scaffolded with CMake.

## Build Instructions

```sh
mkdir build
cd build
cmake ..
cmake --build .
```

## Run

After building, run the executable from the `build` directory:

```sh
./CppCMakeProject
```
